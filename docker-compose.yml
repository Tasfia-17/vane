services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: vane
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - vane_pg:/var/lib/postgresql/data

volumes:
  vane_pg:
